<div class="userInfoBox">
	<%= render 'shared/userInfoBox' %>
</div>


<div id="profileAbout">*describe yourself in 150 char. Probably <span style="color:red">DB field</span> editable through settings</div>

<!--
<div id="picGallery">
	<%#  <% if@users.any? %>
	<%#  	<% @users.each do |user| %>
	<%#  		<%= link_to gravatar_for(user, :size => 30), user %>
	<%#  	<% end %>
	<%#  <% end %>
</div>
-->

<div id="follow_unfollow">
	<%= render 'follow_form' if signed_in? %>
</div>


<div class="feed"> <!--can change class name -->

	<div id="feedTitle"><%= @title %></div>

	<div><%= render 'members' %></div>

</div>

