$("follow_form").update("<%= escape_javascript(render('users/follow')) %>")
//$("numFollowers").update('<%= "#{@user.followers.count}" %>')

if ('<%= "#{@user.followers.count}" %>' == 1) {
	$("numFollowers").update('<%= "#{@user.followers.count}" %>');
	$("txtFollowers").update("follower"); 
}
else {
	$("numFollowers").update('<%= "#{@user.followers.count}" %>');
	$("txtFollowers").update("followers");
}