<div id="infoBox_Container">
<% @user ||= current_user %>

	<div id="handle">
	<!--
	<a href="<%= user_path(current_user) %>"><div>
	<div id="infoBox_Gravatar">
		<%#  <%= gravatar_for current_user, :size => 70 %>
	</div>
	-->
	<a href="<%= user_path(@user) %>"><div>
	<div id="infoBox_Gravatar">
		<%= gravatar_for @user, :size => 70 %>
	</div>


	<div id="infoBox_info">
	<table>
		<tr>
			<!--<td><span id="infoBox_name"><%= current_user.name %></span></td>-->
			<td><span id="infoBox_name"><%= @user.name %></span></td>
		</tr>
		<!--<tr>
			<%#   <td><%= link_to "View my profile", user_path(current_user) %></td>    %>
		</tr>-->		
		<tr>
			<!--<%# <td><%= pluralize(current_user.microposts.count, "post") %></td> -->
			<td><%= pluralize(@user.microposts.count, "post") %></td>
		</tr>
	</table>
	</div>
	</div></a>
</div>

<div id="infoBox_follow">
<% @user ||= current_user %> <!-- Lesson73 @10:40 -->
	<a href="<%= following_user_path(@user) %>" >
	<div id="following">
		<strong><%= @user.following.count %></strong><br> following
	</div>
	</a>

	<a href="<%= followers_user_path(@user) %>" >
	<div id="followers">
		<% if @user.followers.count == 1 %>
			<span id="numFollowers"><%= @user.followers.count %></span> follower <!-- @50:00 ajax-->
		<% else %>
			<span id="numFollowers"><%= @user.followers.count %></span> followers		
		<% end %>
	</div>
	</a>
</div>


</div>